# ============================================================================
# Voice-Enabled RAG over ai4bharat/MSMARCO-XI
# Pinned to the versions this repo was actually built and benchmarked against.
# ----------------------------------------------------------------------------
# NOTE ON TORCH: install torch FIRST and pick the build that matches your
# hardware, because the default PyPI wheel on Linux pulls ~2.5GB of CUDA libs.
#   CPU-only:  pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.4: pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu124
# ============================================================================

# ---- web / API ----
fastapi==0.118.0
uvicorn[standard]==0.37.0
python-multipart==0.0.20
websockets==15.0.1
httpx==0.28.1

# ---- config / validation ----
pydantic==2.10.3
pydantic-settings==2.11.0
python-dotenv==1.1.0
PyYAML>=6.0.1

# ---- models: embedding (BGE-M3 dense+sparse), reranker, NLI ----
# BGE-M3 dense+sparse is implemented directly on transformers (see
# app/retrieval/embedder.py) instead of via FlagEmbedding: fewer transitive
# deps, explicit batching/thread control, and no duplicated model loads.
torch==2.9.1
transformers==4.56.2
tokenizers==0.22.1
safetensors==0.6.2
huggingface-hub==0.35.3

# ---- vector database ----
qdrant-client==1.19.0

# ---- dataset streaming (parquet row-group streaming over HTTP range reads) ----
# `datasets` is deliberately NOT required: MSMARCO-XI ships a legacy loading
# script that datasets>=4 refuses to execute, and we must stream row groups
# rather than materialise 55.6GB. pyarrow + HfFileSystem does this directly.
pyarrow==19.0.0
numpy==2.1.3

# ---- generation ----
groq==1.0.0

# ---- evaluation / reporting ----
scikit-learn==1.7.2
tabulate==0.9.0

# ---- tests ----
pytest==9.1.1
pytest-asyncio==1.4.0
